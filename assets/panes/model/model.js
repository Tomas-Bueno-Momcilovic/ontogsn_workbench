// assets/panes/model/model.js
export { mount, resume, suspend, unmount, renderModelView } from "./model.controller.js";

import { mount, resume, suspend, unmount, renderModelView } from "./model.controller.js";
export default { mount, resume, suspend, unmount, renderModelView };